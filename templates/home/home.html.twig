<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Klask</title>
    <link rel="icon" href="{{ asset('images/favicon.png') }}">
    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
</head>
<body class="page-splash">

    <img src="{{ asset('images/logoklaskfinitsombrepetit.webp') }}" alt="Logo Klask" class="splash-logo">
    <h1 class="splash-title">Klask</h1>
    <p class="splash-sub">Découvrez votre parcours métier</p>

    <div class="progress-bar-wrap">
        <div class="progress-bar-fill"></div>
    </div>

    <script>
        setTimeout(() => { window.location.href = '/inscription'; }, 500);
    </script>
</body>
</html>
